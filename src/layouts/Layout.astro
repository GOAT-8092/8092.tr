---
import '@fontsource/atkinson-hyperlegible/400.css';
import '@fontsource/atkinson-hyperlegible/700.css';
import '@fontsource/atkinson-hyperlegible/400-italic.css';
import '@fontsource/atkinson-hyperlegible/700-italic.css';
import '../styles/global.css';
import PerformanceMonitor from '../components/PerformanceMonitor.astro';
import PWAInstall from '../components/PWAInstall.astro';
import AccessibilityToolbar from '../components/AccessibilityToolbar.astro';
import LanguageSwitcher from '../components/LanguageSwitcher.astro';
import { getCurrentLanguage, getTranslation, getTranslations, type Language, removeLangFromPath } from '../lib/i18n';
import { translations } from '../lib/translations';

export interface Props {
  title: string;
  description?: string;
}

const { title, description = 'FRC Team 8092 - Greatest of All Times' } = Astro.props;
const currentLang = getCurrentLanguage(Astro);
const t = (key: string) => getTranslation(translations, currentLang, key);

// SEO metadata configuration
const seoMetadata = {
  description: description || 'FRC Team 8092 - Greatest of All Times: Tekirdağ\'ın ilk ve tek FRC takımı. 2019\'dan beri FIRST değerleriyle geleceği inşa ediyoruz.',
  keywords: 'FRC, FIRST Robotics Competition, STEM, Robotics, Turkey, Tekirdağ, GOAT 8092, Robotik, Takım',
  ogLocale: currentLang === 'en' ? 'en_US' : 'tr_TR'
};

const currentLocale = currentLang === 'en' ? 'en-US' : 'tr-TR';
---

<!DOCTYPE html>
<html lang={currentLang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <meta name="description" content={description || seoMetadata.description} />
    <title>{title}</title>

    <!-- SEO Meta Tags -->
    <meta name="keywords" content={seoMetadata.keywords} />
    <meta name="author" content="FRC Team 8092" />
    <meta name="robots" content="index, follow" />
    <meta name="language" content={currentLocale} />
    <meta name="geo.region" content="TR-59" />
    <meta name="geo.placename" content="Tekirdağ, Turkey" />
    <meta name="geo.position" content="40.9782;27.5152" />
    <meta name="ICBM" content="40.9782, 27.5152" />
    <meta name="rating" content="general" />
    <meta name="distribution" content="global" />

    <!-- International SEO -->
    <link rel="canonical" href={Astro.url.href} />
    <link rel="alternate" hreflang="tr-TR" href={`https://8092.tr${removeLangFromPath(Astro.url.pathname)}`} />
    <link rel="alternate" hreflang="en-US" href={`https://8092.tr/en${removeLangFromPath(Astro.url.pathname)}`} />
    <link rel="alternate" hreflang="x-default" href={`https://8092.tr${removeLangFromPath(Astro.url.pathname)}`} />

    <!-- Open Graph Meta Tags -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description || seoMetadata.description} />
    <meta property="og:image" content="https://8092.tr/og-cover.jpg" />
    <meta property="og:url" content={Astro.url.href} />
    <meta property="og:site_name" content="FRC Team 8092" />
    <meta property="og:locale" content={seoMetadata.ogLocale} />

    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content="https://8092.tr/og-cover.jpg" />

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": ["Organization", "SportsTeam", "EducationalOrganization"],
      "name": "G.O.A.T. 8092",
      "alternateName": ["GOAT 8092", "Greatest of All Times 8092", "FRC Team 8092"],
      "url": "https://8092.tr/",
      "logo": "https://8092.tr/logo-black.svg",
      "image": "https://8092.tr/og-cover.jpg",
      "foundingDate": "2019-06",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "Veliköy OSB Mesleki ve Teknik Anadolu Lisesi",
        "addressLocality": "Çerkezköy",
        "addressRegion": "Tekirdağ",
        "addressCountry": "TR",
        "postalCode": "59500"
      },
      "contactPoint": {
        "@type": "ContactPoint",
        "email": "iletisim@8092.tr",
        "contactType": "customer service",
        "areaServed": "TR",
        "availableLanguage": "Turkish"
      },
      "description": "Tekirdağ'ın ilk ve tek FRC takımı. 2019'dan beri FIRST değerleriyle geleceği inşa ediyoruz. STEM eğitimi ve robotik yarışmalarında öncü takım.",
      "keywords": "FRC, FIRST Robotics Competition, STEM, Robotics, Turkey, Tekirdağ",
      "sport": "Robotics Competition",
      "memberOf": {
        "@type": "Organization",
        "name": "FIRST",
        "url": "https://www.firstinspires.org"
      },
      "parentOrganization": {
        "@type": "EducationalOrganization",
        "name": "Veliköy OSB Mesleki ve Teknik Anadolu Lisesi"
      },
      "sameAs": [
        "https://www.instagram.com/goat8092/",
        "https://x.com/goat8092",
        "https://www.linkedin.com/company/greatest-of-all-times-8092/",
        "https://www.youtube.com/@greatestofalltimes8092"
      ],
      "knowsAbout": ["Robotics", "STEM Education", "Engineering", "Programming", "Teamwork"],
      "areaServed": {
        "@type": "Country",
        "name": "Turkey"
      }
    }
    </script>
    <!-- Preload critical resources for better Core Web Vitals -->
    <link rel="preload" href="https://fonts.googleapis.com/css2?family=Atkinson+Hyperlegible:wght@400;700&display=swap" as="style" />
    <link href="https://fonts.googleapis.com/css2?family=Atkinson+Hyperlegible:wght@400;700&display=swap" rel="stylesheet" />
    <link rel="preload" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" as="style" />
    <link rel="preload" href="https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0/dist/fancybox/fancybox.css" as="style" />
    <link rel="preload" href="/logo-black.svg" as="image" />
    <link rel="preload" href="/logo-white.svg" as="image" />

    <!-- DNS prefetch for external resources -->
    <link rel="dns-prefetch" href="https://cdnjs.cloudflare.com" />
    <link rel="dns-prefetch" href="https://cdn.jsdelivr.net" />
    <link rel="dns-prefetch" href="https://fonts.googleapis.com" />
    <link rel="dns-prefetch" href="https://fonts.gstatic.com" />

    <!-- Connect preconnect for faster resource loading -->
    <link rel="preconnect" href="https://cdnjs.cloudflare.com" />
    <link rel="preconnect" href="https://cdn.jsdelivr.net" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="preconnect" href="https://code.jquery.com" />

    <link rel="icon" href="/logo-black.svg" type="image/svg+xml">
    <link rel="icon" href="/logo-white.svg" type="image/svg+xml" media="(prefers-color-scheme: dark)">
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#512f75">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="GOAT 8092">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0/dist/fancybox/fancybox.css">
      </head>
  <body id="anasayfa" class="bg-gray-50">
    <slot />

    <!-- Performance Monitoring Component -->
    <PerformanceMonitor />

    <!-- PWA Install Prompt -->
    <PWAInstall />

    <!-- Accessibility Toolbar -->
    <AccessibilityToolbar />

    <script src="https://code.jquery.com/jquery-3.6.0.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/@fancyapps/ui@5.0/dist/fancybox/fancybox.umd.js" defer></script>

    <script is:inline>
      // Initialize Fancybox
      if (typeof Fancybox !== 'undefined') {
        Fancybox.bind('[data-fancybox="gallery"]', {
          // Custom options if needed
        });
      }

      // Smooth scrolling function
      function scrollWindowTo(id) {
        const element = document.getElementById(id);
        if (element) {
          const offset = 80; // Navbar height
          const elementPosition = element.offsetTop - offset;
          window.scrollTo({
            top: elementPosition,
            behavior: 'smooth'
          });
        }
      }

      // Page down function
      function pagedown() {
        const element = document.getElementById('hakkimizda');
        if (element) {
          element.scrollIntoView({ behavior: 'smooth' });
        }
      }

      // Mobile menu functions
      function toggleMobileMenu() {
        const btn = document.getElementById('mobile-menu-button');
        const menu = document.getElementById('mobile-menu');
        const icon = document.getElementById('mobile-menu-icon');

        if (!menu) return;

        if (menu.classList.contains('hidden')) {
          menu.classList.remove('hidden');
          btn?.setAttribute('aria-expanded', 'true');
          icon?.classList.remove('fa-bars');
          icon?.classList.add('fa-times');
          document.body.classList.add('menu-open');
        } else {
          menu.classList.add('hidden');
          btn?.setAttribute('aria-expanded', 'false');
          icon?.classList.remove('fa-times');
          icon?.classList.add('fa-bars');
          document.body.classList.remove('menu-open');
        }
      }

      function closeMobileMenu() {
        const btn = document.getElementById('mobile-menu-button');
        const menu = document.getElementById('mobile-menu');
        const icon = document.getElementById('mobile-menu-icon');

        if (!menu) return;
        menu.classList.add('hidden');
        btn?.setAttribute('aria-expanded', 'false');
        icon?.classList.remove('fa-times');
        icon?.classList.add('fa-bars');
        document.body.classList.remove('menu-open');
      }

      // Mobile menu event listeners
      document.addEventListener('DOMContentLoaded', function() {
        const btn = document.getElementById('mobile-menu-button');
        const menu = document.getElementById('mobile-menu');

        // Dışarı tıklayınca kapat
        document.addEventListener('click', (e) => {
          if (!menu || menu.classList.contains('hidden')) return;
          if (menu.contains(e.target) || btn.contains(e.target)) return;
          closeMobileMenu();
        });

        // ESC ile kapat
        document.addEventListener('keydown', (e) => {
          if (e.key === 'Escape') closeMobileMenu();
        });

        // Ekran >= lg olunca otomatik kapat
        const mq = window.matchMedia('(min-width: 1024px)');
        mq.addEventListener?.('change', (ev) => { if (ev.matches) closeMobileMenu(); });
      });

      
      // Make functions globally available
      window.scrollWindowTo = scrollWindowTo;
      window.pagedown = pagedown;
      window.toggleMobileMenu = toggleMobileMenu;
      window.closeMobileMenu = closeMobileMenu;

      // Register Service Worker
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
          navigator.serviceWorker.register('/sw.js')
            .then(registration => {
              console.log('Service Worker registered successfully:', registration);
            })
            .catch(error => {
              console.log('Service Worker registration failed:', error);
            });
        });
      }
    </script>
  </body>
</html>