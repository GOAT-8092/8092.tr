---
// Contact.astro - İletişim (Contact) section component
---

<section id="iletisim" class="pt-32 pb-20 bg-gray-50 scroll-mt-24">
  <div class="container mx-auto px-4">
    <h2 class="text-4xl font-bold text-center text-gray-800 mb-16">İletişim</h2>
    <div class="max-w-6xl mx-auto">

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">

        <!-- Contact Information -->
        <div class  ="space-y-8">
          <div>
            <h3 class="text-2xl font-bold text-gray-800 mb-6">Bizimle İletişime Geçin</h3>
            <p class="text-lg text-gray-600 mb-8">
              G.O.A.T. 8092 olarak sorularınızı yanıtlamak, projelerimiz hakkında bilgi vermek ve
              sponsorluk imkanlarını değerlendirmek için buradayız. Bizimle iletişime geçmekten çekinmeyin!
            </p>
          </div>

          <!-- Contact Methods -->
          <div class="space-y-6">
            <!-- Email -->
            <div class="flex items-start space-x-4">
              <div class="glass-nav-item bg-primary/80 text-white p-3 rounded-full border-0">
                <i class="fas fa-envelope text-xl"></i>
              </div>
              <div>
                <h4 class="text-lg font-bold text-gray-800">E-posta</h4>
                <p class="text-gray-600">goat8092@gmail.com</p>
                <a href="mailto:goat8092@gmail.com" class="text-primary hover:text-secondary transition-colors">
                  E-posta gönder
                </a>
              </div>
            </div>

            <!-- Address -->
            <div class="flex items-start space-x-4">
              <div class="glass-nav-item bg-primary/80 text-white p-3 rounded-full border-0">
                <i class="fas fa-map-marker-alt text-xl"></i>
              </div>
              <div>
                <h4 class="text-lg font-bold text-gray-800">Adres</h4>
                <p class="text-gray-600">
                  Veliköy OSB Mesleki ve Teknik Anadolu Lisesi<br>
                  Çerkezköy, Tekirdağ<br>
                  Türkiye
                </p>
                <a href="https://maps.app.goo.gl/iJsZm8d5EyumDyw97" target="_blank" class="text-primary hover:text-secondary transition-colors">
                  Haritada görüntüle
                </a>
              </div>
            </div>

            <!-- Social Media -->
            <div class="flex items-start space-x-4">
              <div class="glass-nav-item bg-primary/80 text-white p-3 rounded-full border-0">
                <i class="fas fa-share-alt text-xl"></i>
              </div>
              <div>
                <h4 class="text-lg font-bold text-gray-800">Sosyal Medya</h4>
                <div class="flex space-x-4 mt-2">
                  <a href="https://www.instagram.com/goat8092/" class="text-gray-600 hover:text-primary text-2xl transition-colors" target="_blank">
                    <i class="fab fa-instagram"></i>
                  </a>
                  <a href="https://x.com/goat8092" class="text-gray-600 hover:text-primary text-2xl transition-colors" target="_blank">
                    <i class="fab fa-twitter"></i>
                  </a>
                  <a href="https://www.linkedin.com/company/greatest-of-all-times-8092/" class="text-gray-600 hover:text-primary text-2xl transition-colors" target="_blank">
                    <i class="fab fa-linkedin"></i>
                  </a>
                  <a href="https://www.youtube.com/@greatestofalltimes8092" class="text-gray-600 hover:text-primary text-2xl transition-colors" target="_blank">
                    <i class="fab fa-youtube"></i>
                  </a>
                </div>
              </div>
            </div>
          </div>

          <!-- Quick Links -->
          <div class="bg-white rounded-lg p-6 shadow-lg">
            <h4 class="text-lg font-bold text-gray-800 mb-4">Hızlı Erişim</h4>
            <div class="grid grid-cols-2 gap-4">
              <a href="#hakkimizda" class="text-primary hover:text-secondary transition-colors" onclick="scrollWindowTo('hakkimizda')">
                <i class="fas fa-info-circle mr-2"></i>Hakkımızda
              </a>
              <a href="#takim" class="text-primary hover:text-secondary transition-colors" onclick="scrollWindowTo('takim')">
                <i class="fas fa-users mr-2"></i>Takımımız
              </a>
              <a href="#projelerimiz" class="text-primary hover:text-secondary transition-colors" onclick="scrollWindowTo('projelerimiz')">
                <i class="fas fa-project-diagram mr-2"></i>Projelerimiz
              </a>
              <a href="#galeri" class="text-primary hover:text-secondary transition-colors" onclick="scrollWindowTo('galeri')">
                <i class="fas fa-images mr-2"></i>Galeri
              </a>
            </div>
          </div>
        </div>

        <!-- Contact Form -->
        <div class="bg-white rounded-lg shadow-lg p-8">
          <h3 class="text-2xl font-bold text-gray-800 mb-6">Mesaj Gönderin</h3>
          <form action="https://formspree.io/f/xrbawrgv" method="POST" class="space-y-6" id="contact-form">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="firstName" class="block text-sm font-medium text-gray-700 mb-2">Ad</label>
                <input
                  type="text"
                  id="firstName"
                  name="firstName"
                  required
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                  placeholder="Adınız"
                >
              </div>
              <div>
                <label for="lastName" class="block text-sm font-medium text-gray-700 mb-2">Soyad</label>
                <input
                  type="text"
                  id="lastName"
                  name="lastName"
                  required
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                  placeholder="Soyadınız"
                >
              </div>
            </div>

            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 mb-2">E-posta</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                placeholder="eposta@adresiniz.com"
              >
            </div>

            <div>
              <label for="phone" class="block text-sm font-medium text-gray-700 mb-2">Telefon (İsteğe bağlı)</label>
              <input
                type="tel"
                id="phone"
                name="phone"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
                placeholder="+90 XXX XXX XX XX"
              >
            </div>

            <div>
              <label for="subject" class="block text-sm font-medium text-gray-700 mb-2">Konu</label>
              <select
                id="subject"
                name="subject"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all"
              >
                <option value="">Konu seçiniz</option>
                <option value="sponsorship">Sponsorluk</option>
                <option value="collaboration">İş Birliği</option>
                <option value="membership">Takım Üyeliği</option>
                <option value="information">Bilgi Talebi</option>
                <option value="other">Diğer</option>
              </select>
            </div>

            <div>
              <label for="message" class="block text-sm font-medium text-gray-700 mb-2">Mesaj</label>
              <textarea
                id="message"
                name="message"
                rows="6"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all resize-none"
                placeholder="Mesajınızı buraya yazınız..."
              ></textarea>
            </div>

            <div class="flex items-start space-x-3">
              <input
                type="checkbox"
                id="consent"
                name="consent"
                required
                class="mt-1 w-4 h-4 text-primary border-gray-300 rounded focus:ring-primary"
              >
              <label for="consent" class="text-sm text-gray-600">
                Kişisel verilerimin işlenmesine ve iletişim kurulmasına onay veriyorum.
              </label>
            </div>

            <button
              type="submit"
              class="w-full bg-gradient-to-r from-primary to-secondary text-white py-3 px-6 rounded-lg hover:from-secondary hover:to-accent transition-all duration-300 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2"
            >
              <i class="fas fa-paper-plane mr-2"></i>
              Mesaj Gönder
            </button>
          </form>
        </div>

      </div>

      <!-- Sponsors & Partners -->
      <div class="mt-16">
        <h3 class="text-2xl font-bold text-center text-gray-800 mb-8">Sponsorlarımız ve Ortaklarımız</h3>
        <div class="bg-white rounded-lg shadow-lg p-8">
          <div class="grid grid-cols-2 md:grid-cols-4 gap-8 items-center justify-items-center">
            <div class="text-center">
              <div class="w-20 h-20 rounded-lg flex items-center justify-center mb-2">
                <img src="/sponsors/logo/BSH.png" alt="B/S/H/ Logo" class="max-h-12">
              </div>
              <p class="text-sm text-gray-600">B/S/H/ Türkiye</p>
            </div>
            <div class="text-center">
              <div class="w-20 h-20 rounded-lg flex items-center justify-center mb-2">
                <img src="/sponsors/logo/Kodluyoruz.png" alt="Kodluyoruz Logo" class="max-h-12">
              </div>
              <p class="text-sm text-gray-600">Kodluyoruz</p>
            </div>
            <div class="text-center">
              <div class="w-20 h-20 rounded-lg flex items-center justify-center mb-2">
                <img src="/sponsors/logo/CetinGroup.png" alt="Çetin Group Logo" class="max-h-12">
              </div>
              <p class="text-sm text-gray-600">Çetin Group</p>
            </div>
            <div class="text-center">
              <div class="w-20 h-20 rounded-lg flex items-center justify-center mb-2">
                <img src="/sponsors/logo/SarayAluminyum.png" alt="Saray Alüminyum Logo" class="max-h-12">
              </div>
              <p class="text-sm text-gray-600">Saray Alüminyum</p>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<script>
  // Contact form handling with FormSpree
  document.addEventListener('DOMContentLoaded', function() {
    const contactForm = document.getElementById('contact-form') as HTMLFormElement;

    if (contactForm) {
      contactForm.addEventListener('submit', function(e) {
        e.preventDefault();

        // Show loading state
        const submitButton = contactForm.querySelector('button[type="submit"]') as HTMLButtonElement;
        const originalText = submitButton.innerHTML;
        submitButton.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Gönderiliyor...';
        submitButton.disabled = true;

        // Get form data and submit to FormSpree
        const formData = new FormData(contactForm);

        fetch(contactForm.action, {
          method: 'POST',
          body: formData,
          headers: {
            'Accept': 'application/json'
          }
        })
        .then(response => {
          if (response.ok) {
            return response.json();
          } else {
            throw new Error('Form submission failed');
          }
        })
        .then(() => {
          // Show success message
          const successMessage = document.createElement('div');
          successMessage.className = 'bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4';
          successMessage.innerHTML = '<i class="fas fa-check-circle mr-2"></i>Mesajınız başarıyla gönderildi! En kısa sürede size geri dönüş yapacağız.';

          contactForm.insertBefore(successMessage, contactForm.firstChild);

          // Reset form
          contactForm.reset();

          // Remove success message after 10 seconds
          setTimeout(() => {
            successMessage.remove();
          }, 10000);
        })
        .catch(() => {
          // Show error message
          const errorMessage = document.createElement('div');
          errorMessage.className = 'bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4';
          errorMessage.innerHTML = '<i class="fas fa-exclamation-circle mr-2"></i>Bir hata oluştu. Lütfen daha sonra tekrar deneyin veya doğrudan goat8092@gmail.com adresine e-posta gönderin.';

          contactForm.insertBefore(errorMessage, contactForm.firstChild);

          // Remove error message after 10 seconds
          setTimeout(() => {
            errorMessage.remove();
          }, 10000);
        })
        .finally(() => {
          // Reset button state
          submitButton.innerHTML = originalText;
          submitButton.disabled = false;
        });
      });
    }
  });
</script>