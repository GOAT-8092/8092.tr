---
import { Image } from 'astro:assets';

// Import gallery images
import resim4 from '../assets/gallery/resim4.jpg';
import resim4_1 from '../assets/gallery/resim4.1.jpg';
import resim5_1 from '../assets/gallery/resim5.1.jpg';
import resim6 from '../assets/gallery/resim6.jpg';
import resim6_1 from '../assets/gallery/resim6.1.jpg';
import resim7 from '../assets/gallery/resim7.jpg';
import resim7_1 from '../assets/gallery/resim7.1.jpg';
import resim8 from '../assets/gallery/resim8.jpg';
import resim8_1 from '../assets/gallery/resim8.1.jpg';
import resim9 from '../assets/gallery/resim9.jpg';
import resim10 from '../assets/gallery/resim10.jpg';
import resim10_1 from '../assets/gallery/resim10.1.jpg';
import resim13 from '../assets/gallery/resim13.jpg';
import resim13_1 from '../assets/gallery/resim13.1.jpg';
import resim14 from '../assets/gallery/resim14.jpg';
import resim14_1 from '../assets/gallery/resim14.1.jpg';
import resim15 from '../assets/gallery/resim15.jpg';
import resim15_1 from '../assets/gallery/resim15.1.jpg';
import resim16 from '../assets/gallery/resim16.jpg';
import resim16_1 from '../assets/gallery/resim16.1.jpg';

// Gallery items with categories
const galleryItems = [
  { src: resim4, full: resim4_1, category: 'robots', title: '2025 Robot', subtitle: 'REEFSCAPE' },
  { src: resim5_1, full: resim5_1, category: 'robots', title: 'Robot Detay', subtitle: 'Mekanizma' },
  { src: resim6, full: resim6_1, category: 'robots', title: '2024 Robot', subtitle: 'Crescendo' },
  { src: resim7, full: resim7_1, category: 'team', title: 'Takım 2025', subtitle: 'G.O.A.T. 8092' },
  { src: resim8, full: resim8_1, category: 'team', title: 'Workshop', subtitle: 'Takım Çalışması' },
  { src: resim9, full: resim9, category: 'team', title: 'Mentorlar', subtitle: 'Deneyim Paylaşımı' },
  { src: resim10, full: resim10_1, category: 'competitions', title: 'Bosphorus Regional', subtitle: '2025' },
  { src: resim13, full: resim13_1, category: 'competitions', title: 'Yarışma', subtitle: 'Aksiyonda' },
  { src: resim14, full: resim14_1, category: 'competitions', title: 'Rookie All Star', subtitle: '2020 Ödülü' },
  { src: resim15, full: resim15_1, category: 'outreach', title: 'STEM Workshop', subtitle: 'Eğitim' },
  { src: resim16, full: resim16_1, category: 'outreach', title: 'G.O.A.T. Jr.', subtitle: 'Mentorluk' },
  { src: resim4, full: resim4_1, category: 'outreach', title: 'Toplum Etkinliği', subtitle: 'Farkındalık' },
  { src: resim6, full: resim6_1, category: 'team', title: 'Takım Geliştirme', subtitle: 'Etkinlik' },
  { src: resim8, full: resim8_1, category: 'robots', title: 'CAD Tasarım', subtitle: '3D Modelleme' },
  { src: resim10, full: resim10_1, category: 'competitions', title: 'Pit Alanı', subtitle: 'Hazırlık' },
];
---
<script>
  document.addEventListener('DOMContentLoaded', function() {
    Fancybox.bind('[data-fancybox="gallery"]', {});
  });
</script>

<section id="galeri" class="pt-32 pb-20 scroll-mt-24">
  <div class="container mx-auto px-4">
    <h2 class="text-4xl font-bold text-center text-gray-800 mb-16">Galeri</h2>
    <div class="max-w-6xl mx-auto">

      <!-- Gallery Filter -->
      <div class="flex flex-wrap justify-center gap-4 mb-12">
        <button class="gallery-filter active bg-primary glass-nav-item text-white" data-filter="*">
          Tümü
        </button>
        <button class="gallery-filter bg-gray-200 glass-nav-item text-gray-700" data-filter=".robots">
          Robotlar
        </button>
        <button class="gallery-filter bg-gray-200 glass-nav-item text-gray-700" data-filter=".team">
          Takım
        </button>
        <button class="gallery-filter bg-gray-200 glass-nav-item text-gray-700" data-filter=".competitions">
          Yarışmalar
        </button>
        <button class="gallery-filter bg-gray-200 glass-nav-item text-gray-700" data-filter=".outreach">
          Toplum Hizmeti
        </button>
      </div>

      <!-- Gallery Grid -->
      <div class="gallery-grid grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
        {galleryItems.map((item) => (
          <div class={`gallery-item ${item.category}`}>
            <a href={item.full.src} data-fancybox="gallery" data-caption={`${item.title} - ${item.subtitle}`}> 
              <div class="relative overflow-hidden rounded-lg shadow-lg hover:shadow-xl transition-shadow duration-300 group">
                <img
                  src={item.src.src}
                  alt={`${item.title} - ${item.subtitle}`}
                  width="400"
                  height="300"
                  loading="lazy"
                  class="w-full h-64 object-cover group-hover:scale-110 transition-transform duration-500"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                  <div class="absolute bottom-4 left-4 text-white">
                    <h4 class="font-bold">{item.title}</h4>
                    <p class="text-sm">{item.subtitle}</p>
                  </div>
                  <div class="absolute top-4 right-4 text-white">
                    <i class="fas fa-expand-alt"></i>
                  </div>
                </div>
              </div>
            </a>
          </div>
        ))}
      </div>

      <!-- Gallery Stats -->
      <div class="mt-16 bg-gradient-to-r from-primary to-secondary text-white rounded-lg p-8">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8 text-center">
          <div>
            <h3 class="text-3xl font-bold mb-2">6+</h3>
            <p class="text-lg">Yıl Deneyim</p>
          </div>
          <div>
            <h3 class="text-3xl font-bold mb-2">5+</h3>
            <p class="text-lg">Robot Tasarımı</p>
          </div>
          <div>
            <h3 class="text-3xl font-bold mb-2">100+</h3>
            <p class="text-lg">Fotoğraf</p>
          </div>
          <div>
            <h3 class="text-3xl font-bold mb-2">1000+</h3>
            <p class="text-lg">Anı</p>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<script>
  // Gallery filter functionality
  document.addEventListener('DOMContentLoaded', function() {
    const filterButtons = document.querySelectorAll<HTMLButtonElement>('.gallery-filter');
    const galleryItems = document.querySelectorAll<HTMLElement>('.gallery-item');

    filterButtons.forEach(button => {
      button.addEventListener('click', (event) => {
        // Remove active class from all buttons
        filterButtons.forEach(btn => {
          btn.classList.remove('active', 'bg-primary', 'text-white');
          btn.classList.add('bg-gray-200', 'text-gray-700');
        });

        // Add active class to clicked button
        const target = event.currentTarget as HTMLButtonElement;
        target.classList.add('active', 'bg-primary', 'text-white');
        target.classList.remove('bg-gray-200', 'text-gray-700');

        // Get filter value
        const filterValue = target.getAttribute('data-filter');

        // Show/hide gallery items
        galleryItems.forEach(item => {
          if (filterValue === '*' || (filterValue && item.classList.contains(filterValue.substring(1)))) {
            (item.style as CSSStyleDeclaration).display = 'block';
            setTimeout(() => {
              (item.style as CSSStyleDeclaration).opacity = '1';
              (item.style as CSSStyleDeclaration).transform = 'scale(1)';
            }, 10);
          } else {
            (item.style as CSSStyleDeclaration).opacity = '0';
            (item.style as CSSStyleDeclaration).transform = 'scale(0.8)';
            setTimeout(() => {
              (item.style as CSSStyleDeclaration).display = 'none';
            }, 300);
          }
        });
      });
    });

    // Initialize gallery items with transition
    galleryItems.forEach(item => {
      (item.style as CSSStyleDeclaration).transition = 'opacity 0.3s ease, transform 0.3s ease';
    });
  });
</script>