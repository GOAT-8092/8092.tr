---
---

<nav class="fixed w-full z-50 glass-nav">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-2">
      <div class="flex items-center space-x-3">
        <img src="/img/goat_logo_-_beyaz2.png" alt="GOAT Logo" class="h-12 w-12 cursor-pointer transition-transform hover:scale-110"
          onclick="scrollWindowTo('anasayfa')">
        <div class="text-lg font-bold text-gray-800" id="logo-text">
          G.O.A.T. #8092
        </div>
      </div>

      <button class="lg:hidden glass-nav-item p-2" type="button" onclick="toggleMobileMenu()">
        <i class="fas fa-bars text-gray-700" id="mobile-menu-icon"></i>
      </button>

      <div class="hidden lg:flex items-center space-x-6" id="navbarSupportedContent">
        <a class="glass-nav-item cursor-pointer"
          onclick="scrollWindowTo('anasayfa')">Anasayfa</a>
        <a class="glass-nav-item cursor-pointer"
          onclick="scrollWindowTo('hakkimizda')">Hakkımızda</a>
        <a class="glass-nav-item cursor-pointer"
          onclick="scrollWindowTo('takim')">Takım</a>
        <a class="glass-nav-item cursor-pointer"
          onclick="scrollWindowTo('projelerimiz')">Projelerimiz</a>
        <a class="glass-nav-item cursor-pointer"
          onclick="scrollWindowTo('galeri')">Galeri</a>
        <a class="glass-nav-item cursor-pointer"
          onclick="scrollWindowTo('iletisim')">İletişim</a>
      </div>
    </div>

    <!-- Mobile menu dropdown -->
    <div class="lg:hidden hidden bg-white border-t border-gray-200" id="mobile-menu">
      <div class="px-2 pt-2 pb-3 space-y-1">
        <a class="block px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md cursor-pointer"
          onclick="scrollWindowTo('anasayfa'); closeMobileMenu()">Anasayfa</a>
        <a class="block px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md cursor-pointer"
          onclick="scrollWindowTo('hakkimizda'); closeMobileMenu()">Hakkımızda</a>
        <a class="block px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md cursor-pointer"
          onclick="scrollWindowTo('takim'); closeMobileMenu()">Takım</a>
        <a class="block px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md cursor-pointer"
          onclick="scrollWindowTo('projelerimiz'); closeMobileMenu()">Projelerimiz</a>
        <a class="block px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md cursor-pointer"
          onclick="scrollWindowTo('galeri'); closeMobileMenu()">Galeri</a>
        <a class="block px-3 py-2 text-gray-800 hover:bg-gray-100 rounded-md cursor-pointer"
          onclick="scrollWindowTo('iletisim'); closeMobileMenu()">İletişim</a>
      </div>
    </div>
  </div>
</nav>

<script>
function toggleMobileMenu() {
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = document.getElementById('mobile-menu-icon');

  if (mobileMenu.classList.contains('hidden')) {
    mobileMenu.classList.remove('hidden');
    menuIcon.classList.remove('fa-bars');
    menuIcon.classList.add('fa-times');
  } else {
    mobileMenu.classList.add('hidden');
    menuIcon.classList.remove('fa-times');
    menuIcon.classList.add('fa-bars');
  }
}

function closeMobileMenu() {
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = document.getElementById('mobile-menu-icon');

  mobileMenu.classList.add('hidden');
  menuIcon.classList.remove('fa-times');
  menuIcon.classList.add('fa-bars');
}
</script>