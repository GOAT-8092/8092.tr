---
---

<header>
  <div class="relative min-h-screen overflow-hidden">
    <!-- Background with purple gradient and pattern -->
    <div class="absolute inset-0" style="background: linear-gradient(135deg, #512f75 0%, #673a8e 50%, #7e46a8 100%);"></div>

    <!-- Animated background elements -->
    <div class="absolute inset-0">
      <div class="absolute top-20 left-10 w-32 h-32 border border-white/20 rounded-full animate-pulse"></div>
      <div class="absolute top-40 right-20 w-24 h-24 border-2 border-white/10 rotate-45 animate-spin" style="animation-duration: 20s;"></div>
      <div class="absolute bottom-32 left-1/4 w-16 h-16 bg-white/5 rounded-lg animate-bounce" style="animation-delay: 2s;"></div>
      <div class="absolute top-1/3 right-1/3 w-20 h-20 border border-white/15 rounded-full animate-ping" style="animation-delay: 1s;"></div>
    </div>

    <!-- Main content -->
    <div class="relative h-full flex flex-col items-center justify-center text-white px-4 pt-28 pb-16">
      <!-- FRC Badge -->
      <div class="mb-2 md:mb-4 flex items-center space-x-2 md:space-x-4 animate-fade-in-up" style="animation-delay: 0.2s;">
        <div class="w-10 h-10 md:w-12 md:h-12 bg-white/20 rounded-full flex items-center justify-center backdrop-blur-sm">
          <i class="fas fa-robot text-sm md:text-lg"></i>
        </div>
        <span class="text-xs md:text-sm font-semibold tracking-wider">FIRST ROBOTICS COMPETITION</span>
      </div>

      <!-- Team Name -->
      <div class="text-center space-y-1 md:space-y-2 animate-fade-in-up" style="animation-delay: 0.4s;">
        <h1 class="text-2xl sm:text-4xl md:text-6xl lg:text-7xl font-bold leading-tight">
          <span class="block">Greatest Of</span>
          <span class="block text-yellow-300">All Times</span>
        </h1>

        <!-- Team Number with special styling -->
        <div class="relative my-1 md:my-2">
          <h2 class="text-3xl sm:text-5xl md:text-7xl lg:text-8xl font-black text-white/90 animate-pulse">8092</h2>
          <div class="absolute inset-0 text-3xl sm:text-5xl md:text-7xl lg:text-8xl font-black text-white/20 transform translate-x-0.5 translate-y-0.5">8092</div>
        </div>

        <!-- Subtitle -->
        <h3 class="text-base md:text-xl lg:text-2xl font-light text-blue-200 tracking-widest">FRC TEAM</h3>
      </div>

      <!-- Location and achievements -->
      <div class="mt-2 md:mt-4 text-center space-y-1 animate-fade-in-up" style="animation-delay: 0.6s;">
        <p class="text-sm md:text-base text-white/90">
          <i class="fas fa-map-marker-alt mr-1"></i>
          Tekirdağ, Türkiye
        </p>
        <p class="text-xs md:text-sm text-yellow-300 font-semibold">
          🏆 Rookie All Star Award 2020
        </p>
        <p class="text-xs text-white/80">
          2019'dan beri • Tekirdağ'ın İlk ve Tek FRC Takımı
        </p>
      </div>

      <!-- Action buttons -->
      <div class="mt-4 md:mt-6 flex flex-col sm:flex-row gap-2 md:gap-3 animate-fade-in-up" style="animation-delay: 0.8s;">
        <button
          onclick="scrollWindowTo('hakkimizda')"
          class="px-4 md:px-6 py-2 bg-white/20 backdrop-blur-sm border border-white/30 rounded-full text-white font-semibold hover:bg-white/30 transition-all duration-300 transform hover:scale-105 text-xs md:text-sm"
        >
          <i class="fas fa-info-circle mr-1"></i>
          Hikayemizi Keşfedin
        </button>
        <button
          onclick="scrollWindowTo('takim')"
          class="px-4 md:px-6 py-2 bg-yellow-400/90 text-purple-900 font-semibold rounded-full hover:bg-yellow-300 transition-all duration-300 transform hover:scale-105 text-xs md:text-sm"
        >
          <i class="fas fa-users mr-1"></i>
          Takımımızı Tanıyın
        </button>
      </div>

      <!-- Scroll indicator -->
      <div class="mt-6 md:mt-8 animate-bounce">
        <button onclick="pagedown()" class="text-white/70 hover:text-white transition-colors">
          <div class="flex flex-col items-center space-y-1">
            <span class="text-xs">Aşağı Kaydır</span>
            <i class="fas fa-chevron-down text-sm md:text-lg"></i>
          </div>
        </button>
      </div>
    </div>
  </div>
</header>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }
</style>