---
import { Image } from 'astro:assets';
import goatLogo from '../assets/img/goat_logo_-_beyaz2.png';
import frcLogo from '../assets/img/FRC_Logo.png';
---

<header>
  <div class="relative" style="background: linear-gradient(135deg, #512f75 0%, #673a8e 50%, #7e46a8 100%);">
    <!-- Background pattern -->
    <div class="absolute inset-0 bg-white/10"></div>
    <div class="absolute inset-0" style="background-image: radial-gradient(rgba(255,255,255,.15) 1px, transparent 1px); background-size: 24px 24px;"></div>

    <!-- Main content -->
    <div class="relative">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-20 pb-16 md:pt-28 md:pb-20">
        <div class="mx-auto max-w-4xl text-center">
          <!-- Logo and FRC Badge -->
          <div class="mb-6">
            <Image
              src={goatLogo}
              alt="G.O.A.T. 8092 Logo"
              width={200}
              height={200}
              loading="eager"
              class="mx-auto mb-4 drop-shadow-2xl"
              format="webp"
              quality={90}
            />
            <div class="flex items-center justify-center space-x-2">
              <Image
                src={frcLogo}
                alt="FRC Logo"
                width={40}
                height={40}
                loading="eager"
                class="bg-white/20 rounded-full p-1"
                format="webp"
                quality={80}
              />
              <span class="text-sm text-white/80 tracking-wider uppercase first-badge">FIRST ROBOTICS COMPETITION</span>
            </div>
          </div>

          <!-- Main Headline -->
          <h1 class="text-4xl font-bold tracking-tight text-white sm:text-6xl md:text-7xl">
            <span class="block">Greatest of</span>
            <span class="block text-yellow-300">All Times</span>
            <span class="block text-white text-5xl sm:text-7xl md:text-8xl font-black mt-2">#8092</span>
          </h1>

          <!-- Subtitle -->
          <p class="mx-auto mt-6 max-w-2xl text-lg leading-8 text-white/90">
            Tekirdağ'ın ilk ve tek FRC takımı olarak 2019'dan beri FIRST değerleriyle geleceği inşa ediyoruz. Innovation, teamwork ve gracious professionalism ile robotik dünyasında iz bırakıyoruz.
          </p>

          <!-- Achievement Badge -->
          <div class="mt-4 flex items-center justify-center">
            <div class="inline-flex items-center rounded-full bg-white/20 px-3 py-1 text-sm font-medium text-white ring-1 ring-inset ring-white/30">
              🏆 Rookie All Star Award 2020
            </div>
          </div>

          <!-- Action buttons -->
          <div class="mt-10 flex items-center justify-center gap-x-6">
            <a
              href="#hakkimizda"
              role="button"
              onclick="scrollWindowTo('hakkimizda')"
              class="rounded-md bg-yellow-400 text-purple-900 px-6 py-3 text-sm font-semibold shadow-sm hover:bg-yellow-300 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-yellow-400 transition-colors inline-block"
            >
              Hikayemizi Keşfedin
            </a>
            <a
              href="#takim"
              role="button"
              onclick="scrollWindowTo('takim')"
              class="rounded-md bg-white/20 backdrop-blur-sm px-6 py-3 text-sm font-semibold text-white shadow-sm ring-1 ring-inset ring-white/30 hover:bg-white/30 transition-colors inline-block"
            >
              Takımımızı Tanıyın
            </a>
          </div>

          <!-- Scroll indicator -->
          <div class="mt-16">
            <button onclick="pagedown()" class="text-white/60 hover:text-white transition-colors">
              <div class="flex flex-col items-center space-y-2">
                <span class="text-sm">Aşağı Kaydır</span>
                <i class="fas fa-chevron-down text-lg animate-bounce"></i>
              </div>
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>