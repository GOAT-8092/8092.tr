---
import Iridescence from './Iridescence';
import { getCurrentLanguage, getTranslation } from '../lib/i18n';
import { translations } from '../lib/translations';

const currentLang = getCurrentLanguage(Astro);
const t = (key: string) => getTranslation(translations, currentLang, key);
---

<header>
  <div class="relative min-h-screen" style="background: linear-gradient(135deg, #512f75 0%, #673a8e 50%, rgb(126, 70, 168) 100%);">
    <Iridescence client:load style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%' }}
      color={[0.66, 0.5, 1]}
      mouseReact={false}
      speed={0.2}
    />
    
    
    <!-- Main content -->
    <div class="relative">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-20 pb-16 md:pt-28 md:pb-20">
        <div class="mx-auto max-w-4xl text-center">
          <!-- FRC Badge -->
          <div class="mb-4 flex items-center justify-center space-x-2">
            <div class="w-10 h-10 bg-white/20 rounded-full flex items-center justify-center">
              <i class="fas fa-robot text-white text-sm" aria-hidden="true"></i>
            </div>
            <span class="text-sm text-white/80 tracking-wider uppercase first-badge">{t('hero.frc_badge')}</span>
          </div>
          
          <!-- Main Headline -->
          <h1 class="text-4xl font-bold tracking-tight text-white sm:text-6xl md:text-7xl">
            <span class="block">{t('hero.heading_line1')}</span>
            <span class="block text-yellow-300">{t('hero.heading_line2')}</span>
            <span class="block text-white text-5xl sm:text-7xl md:text-8xl font-black mt-2">{t('hero.heading_tagline')}</span>
          </h1>

          <!-- Subtitle -->
          <p class="mx-auto mt-6 max-w-2xl text-lg leading-8 text-white/90">
            {t('hero.subtitle_long')}
          </p>

          <!-- Achievement Badge -->
          <div class="mt-4 flex items-center justify-center">
            <div class="inline-flex items-center rounded-full bg-white/20 px-3 py-1 text-sm font-medium text-white ring-1 ring-inset ring-white/30">
              🏆 {t('hero.achievement')}
            </div>
          </div>

          <!-- Action buttons -->
          <div class="mt-10 flex items-center justify-center gap-x-6">
            <a
              href="#hakkimizda"
              role="button"
              onclick="scrollWindowTo('hakkimizda')"
              class="rounded-md bg-yellow-400 text-purple-900 px-6 py-3 text-sm font-semibold shadow-sm hover:bg-yellow-300 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-yellow-400 transition-colors inline-block"
            >
              {t('hero.discover_button')}
            </a>
            <a
              href="#takim"
              role="button"
              onclick="scrollWindowTo('takim')"
              class="rounded-md bg-white/20 backdrop-blur-sm px-6 py-3 text-sm font-semibold text-white shadow-sm ring-1 ring-inset ring-white/30 hover:bg-white/30 transition-colors inline-block"
            >
              {t('hero.meet_team_button')}
            </a>
          </div>

          <!-- Scroll indicator -->
          <div class="mt-16">
            <button onclick="pagedown()" class="text-white/60 hover:text-white transition-colors">
              <div class="flex flex-col items-center space-y-2">
                <span class="text-sm">{t('hero.scroll_down')}</span>
                <i class="fas fa-chevron-down text-lg animate-bounce"></i>
              </div>
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>
